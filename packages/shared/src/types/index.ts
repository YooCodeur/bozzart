// @bozzart/shared — types barrel
//
// DRY convention: any TypeScript type, interface, or enum that must be
// consumed by BOTH `apps/web` and `apps/mobile` (or by multiple packages)
// belongs here. Keep this barrel platform-agnostic — no React, no Next,
// no Expo, no Node-only APIs. Import from "@bozzart/shared/types".

export {};
