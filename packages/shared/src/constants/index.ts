// @bozzart/shared — constants barrel
//
// DRY convention: cross-platform constants (enums of string literals,
// config keys, limits, feature flags, route names) shared between web
// and mobile live here. Import from "@bozzart/shared/constants".

export {};
